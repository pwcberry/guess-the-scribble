# Port the Fastify game server (server/src/index.ts) listens on.
PORT=3000

# SQLite database file. Optional: defaults to <repo>/data/gts.db.
# Set an absolute path in production/Docker, or ":memory:" for ephemeral runs.
# DB_FILE=./data/gts.db
